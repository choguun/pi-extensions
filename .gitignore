# Dependencies
node_modules/

# Build artifacts
*.tsbuildinfo
dist/
build/

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
*.swo
*~

# Logs
*.log
npm-debug.log*

# Test artifacts
coverage/
.nyc_output/

# Local env
.env
.env.local

# AIDLC + plan-mode + superpowers local artifacts (per-session, never committed)
# — moved to .git/info/exclude (local-only, doesn't ship in PRs)
# — see AGENTS.md "Local artifacts (not committed)" section for the canonical setup
